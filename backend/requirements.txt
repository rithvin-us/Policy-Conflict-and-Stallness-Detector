# Core backend — installs cleanly on Python 3.11–3.14, no compiler needed.
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7
python-multipart>=0.0.9
httpx>=0.27

# Async workers (optional at runtime — see requirements-ml.txt notes).
celery>=5.3
redis>=5.0

# Postgres driver (used when DATABASE_URL points at Postgres).
psycopg[binary]>=3.1

# SingleStore support (used when DATABASE_URL is singlestore(db)://).
#   singlestoredb          -> DBAPI driver
#   sqlalchemy-singlestoredb -> registers the ``singlestoredb`` SQLAlchemy dialect
#                               (without it, create_engine raises NoSuchModuleError)
singlestoredb>=1.7
sqlalchemy-singlestoredb>=1.2

# Testing
pytest>=8.0
